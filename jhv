def feedback_to_notify_condition(state: WorkflowState):
    user_feedback = state.get("issue_text")
    if user_feedback and isinstance(user_feedback, dict) and user_feedback.get("issue_text"):
        return "notify"
    else:
        return None  # End workflow if no feedback

graph.add_conditional_edges("feedback", feedback_to_notify_condition)
